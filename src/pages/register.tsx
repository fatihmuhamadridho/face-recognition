import { AuthTemplate, Default } from '@components/templates';
import { styles } from '@libs';
import { Text } from '@mantine/core';

export default function Register() {
  return (
    <AuthTemplate title="Dashboard">
    </AuthTemplate>
  );
}
